---
- name: set up nushell for user
  hosts: localhost
  become: false
  vars:
    the_user: "{{ ansible_user_id }}"
  tasks:
    - name: change user shell to nu
      become: true
      user:
        name: "{{ the_user }}"
        shell: /usr/bin/nu
