# My Dotfiles

My dotfiles are managed with [Chezmoi](https://www.chezmoi.io/) and [Ansible](https://docs.ansible.com/).


## Installation

To install my dotfiles, simply paste in the following command:

```bash
  sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply ste1ee-dot
```
